/** Product-wide constants for Kuytu. */

/** The "women first move" window — women must open the chat within this time. */
export const FIRST_MOVE_WINDOW_MS = 48 * 60 * 60 * 1000; // 48 hours

/** Drag distance (px) past which a swipe is committed on release. */
export const SWIPE_COMMIT_THRESHOLD = 120;

/** Upward drag distance (px) that commits a Super Like. */
export const SUPER_LIKE_THRESHOLD = 140;

/** How far off-screen a committed card flies. */
export const SWIPE_FLYOUT_DISTANCE = 1000;
